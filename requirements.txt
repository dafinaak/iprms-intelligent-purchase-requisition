# IPRMS — core dependencies
# Standalone deterministic Python pipeline. No cloud platform required for core scope.

# --- Data processing ---
pandas>=2.0
# PySpark is optional, only if the team wants larger-scale local data processing
# pyspark>=3.5

# --- Schema validation ---
pydantic>=2.5

# --- Config / bundle files (manifest.yaml, policy_pack.yaml) ---
PyYAML>=6.0

# --- Document extraction ---
# PyMuPDF / pdfplumber for clean digital PDFs
pymupdf>=1.24
pdfplumber>=0.11
# OCR engine for scanned PDFs
pytesseract>=0.3.10
Pillow>=10.0

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# --- Demo UI ---
streamlit>=1.33

# --- Testing ---
pytest>=8.0

# --- Optional: LangGraph internal A→H wrapper (project must work without it) ---
langgraph>=0.2

# --- Optional: LangChain / LLM Agent B fallback (project must work without it) ---
langchain>=0.2
